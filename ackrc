# Always sort the files
--sort-files

# Always color, even if piping to another program
--color

# Use "less -r" as my pager
# --pager=less -r  

# Search through Drupal module files
# breaks ack, "unknown option"
# --type-add php:module,inc,install

--smart-case
